chromadb==1.0.20
deep-translator==1.11.4
fastapi==0.116.1
httpx==0.28.1
pytest==8.3.5
redis==5.2.1
# Python 3.13 is the official backend runtime for full semantic retrieval.
# Keep Python 3.14 as a temporary lexical-only fallback until PyTorch wheels are available there.
sentence-transformers==5.1.0; python_version < "3.14"
torch==2.8.0; python_version < "3.14"
uvicorn==0.35.0
